command to login into clound foundry : cf login -a api.run.pivotal.io

Once authentication is successful execute command : cf push

Clound foundry looks for manifest.yml file . And the project gets deployed.